# Runtime deps for sw-process-meeting. Install into an isolated venv:
#   uv venv .venv && uv pip install --python .venv/bin/python -r requirements.txt
assemblyai        # TRANSCRIBE_PROVIDER=assemblyai (default)
openai            # LLM_PROVIDER=openrouter (OpenAI-compatible client)
anthropic         # LLM_PROVIDER=anthropic
python-dotenv     # optional .env fallback when secrets are not injected by a secrets manager
# openai-whisper  # TRANSCRIBE_PROVIDER=whisper-local — heavy (pulls torch); install only if used
